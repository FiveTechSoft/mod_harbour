## Copy .htaccess to c:\Apache\htdocs and httpd.conf to c:\Apache\conf

Then you can use these formats from the browser:

localhost/cgi-bin/modharbour.exe?test.prg

localhost/test.prg

localhost/test
