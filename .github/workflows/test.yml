name: Windows Test

on:
  push:
    branches: [master]

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - name: My Tests
      shell: cmd
      run: |
         dir "c:\%ProgramFiles(x86)%\Microsoft Visual Studio\2019\Enterprise"
         dir "C:\Program Files (x86)\Microsoft Visual Studio 14.0\VC"
      
